# finegray delayed-entry gate transfer pin
#
# The commit whose tree the three ZZF Monte Carlo gates were last actually RUN
# against.  run_all.sh extracts this tree with `git archive' and diffs four
# delayed-entry arms against the tree under test (qa/gates_transfer_proof.do).
# Any change to the delayed-entry weight, score, or variance code perturbs those
# numbers and fails the lane -- which is the point: run_status_gates.txt's own
# invalidation rule fired four times between 2026-07-20 and 2026-08-02 and was
# never honoured, because nothing checked it.
#
# CHANGE THIS PIN ONLY after actually re-running `./run_all.sh gates' (~7h) on
# the new commit and appending the result to run_status_gates.txt.  Moving the
# pin to silence a red transfer gate destroys the only evidence that the gates
# still describe the shipped estimator.

gated_commit: c7ca4445
gated_note:   the tree the three ZZF gates were LAST ACTUALLY RUN on --
              run_status_gates.txt, lane=gates, 2026-09-12T23:12:50Z, 3/3
              (recovery 8/8, coverage 1/1, factorization 10/10), .ado files
              byte-identical to c7ca44454e4755f4542629b22003cd2003864691
              (finegray 1.3.4; the receipt's pkg_tree 23a1ac2e reads
              "uncommitted-changes-present" because qa/test_finegray_zzf.do
              was mid-edit -- it and three doc files were the only
              differences, none of them fitted by a gate).  That run's
              transfer_gate line is FAIL against the PREVIOUS pin cda50792,
              which is the expected shape: 1.3.3's stratified-weight
              normalizer (0700c65b) moved the zzf_fact arm on purpose, and
              1.3.4 (c7ca4445) is the first tree carrying it on which the
              gates passed -- the 2026-09-12 18:18 UTC run on fe0d983c
              (1.3.3) failed coverage (rep 245 refused, r(459)) and
              factorization 2a; both are recorded under "Gates lane
              history" in qa/AUDIT_NOTES.md.
              Moved here from cda50792 on 2026-09-13.
