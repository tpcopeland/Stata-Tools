# finegray QA

The `finegray` QA suite covers estimation, prediction, cumulative-incidence reporting, and proportional-subdistribution-hazards diagnostics through functional, known-answer, and independent-oracle checks. Suites live in a flat `qa/` root, use one curated lane runner, and are independently runnable from this directory.

This file is the runbook: how to run each lane, and one sentence on what each file checks. The retrospective material — why a check exists, what it was measured at, and which defect it was written against — is in [AUDIT_NOTES.md](AUDIT_NOTES.md).

## How to run

```bash
cd finegray/qa
./run_all.sh full --source-repo "$(cd ../.. && pwd)"  # full release gate (preferred)
stata-mp -b do run_all.do quick                       # fast functional lane
stata-mp -b do test_finegray_v121.do                  # one standalone suite
```

`run_all.do` defaults to `full`; `run_all.sh` adds reliable shell exit status, provenance, receipts, and the shell-level fail-closed and transfer gates. Gate on the final `RESULT:` line or the wrapper receipt, not Stata batch mode's shell status alone.

## Isolation

The runner writes suite logs in the shared `qa/` directory, so concurrent runs of the same lane can corrupt evidence; disagreement between `run_all.log` and a suite's own log is the tell. For concurrency, copy the repository layout to a scratch tree containing both `finegray/` and sibling `tabtools/`, delete copied `qa/*.log` and `qa/run_all_status.txt`, and pass the original repository to `run_all.sh --source-repo`.

| Sibling | Required by | Behavior if absent |
|---|---|---|
| `tabtools/` | `test_finegray.do` (`regtab` integration) | Hard failure before the suite publishes a result |
| `_data/` | Repository-level example-data paths | Hard failure when an exercised example needs it |

## Conventions

- `test_*` files are functional or regression checks; `validation_*` files use hand-computable, invariant, simulated-truth, or deterministic Stata oracles; `crossval_*` files compare against independent Stata/R implementations; `benchmark_*` files measure performance and are never correctness gates.
- Every runnable suite ends with one `RESULT: <name> tests=N pass=N fail=N [skip=N]` line and exits nonzero on failure. `full` treats any skipped oracle as a failure.
- `_finegray_qa_common.do` sandboxes `PLUS` and `PERSONAL` under `c(tmpdir)` and restores them, so lane runs do not alter the user's real ado tree.
- Paths derive from `c(pwd)` or wrapper arguments; no suite depends on a machine-local absolute path.
- Test data are generated from seeded builders or downloaded through Stata's documented `webuse` examples; cross-validation fixtures are regenerated by their companion R scripts.
- Generated logs, datasets, CSV files, graphs, caches, and workbooks are gitignored. Tracked receipts and transfer pins are explicit provenance artifacts; documentation images belong under `demo/`.

## Dependencies

| Suite or lane | Needs | If missing |
|---|---|---|
| All lanes | Stata 16+ via `stata-mp` | Hard failure |
| `test_finegray.do` | Sibling `tabtools` package | Hard failure |
| `python` / `full` | `Rscript`; R packages `cmprsk`, `crrSC`, `survival`, `riskRegression`, and `prodlim` | Hard failure; a skip cannot make the lane green |
| Optional acceleration parity | R package `fastcmprsk` | The optional comparison is omitted; core `cmprsk` parity still runs |
| Shell wrapper | POSIX shell utilities and Python 3 | Hard failure of the wrapper gate |
| Every lane | **Internet access** for Stata's `webuse` datasets — `webuse hypoxia` (the shared fixture), `webuse hiv_si` (`over()` and grouped-CIF examples) and `webuse pneumonia` (the internal time-varying-covariate refusal) | `r(672)`/`r(677)` from `webuse` inside the affected suites; the failure is reported against the test, not against the network |

`webuse hypoxia` is cached at `` `c(tmpdir)'/finegray_hypoxia_cache.dta ``. Each suite that uses it defines a local `_finegray_use_hypoxia` helper (`test_finegray.do`, `test_finegray_v110.do`, `validation_finegray.do`, `crossval_finegray.do`, `crossval_cif.do`, `crossval_predict_phtest.do`, `crossval_predict_stcrreg.do`) which downloads once, `save`s the cache, and `use`s it on every later call — so a lane makes one network round trip for that dataset rather than one per suite. Deleting the cache file only costs a re-download; a stale cache cannot arise, because the dataset is a fixed StataCorp example. `webuse hiv_si` and `webuse pneumonia` are not cached and are fetched each time they are used, so those blocks need the network on every run.

## File index

One sentence per file. The full prose that used to sit in these cells is preserved verbatim in [AUDIT_NOTES.md](AUDIT_NOTES.md#per-suite-detail); read it there before weakening, moving or deleting a check.

### Functional and regression tests

| File | Covers |
|---|---|
| `test_finegray.do` | Public-command basics, options, returns, errors, install behavior, and cross-command workflows. |
| `test_finegray_v110.do` | Multiple-record reduction, CIF/bootstrap additions, state preservation, factor profiles, v1.1.x regressions, and the `_fg_*` ownership rule (recognition by per-run characteristic, and adjudication completed before any mutation). |
| `test_finegray_v120.do` | Diagnostic-only `finegray_phtest` return and display contract. |
| `test_finegray_v120b.do` | Factor coefficient naming, replay, reporting, support notes, saving labels, and v1.2.0 presentation regressions. |
| `test_finegray_v121.do` | Cold-cache multiple-record prediction, long factor names, cilevel validation, and consulted-only positivity guards. |
| `test_finegray_v130.do` | Missing-`compete()` refusal (including the `stsplit`-blanked configuration), one `level()` bound across fit/replay/`finegray_cif`/`finegray_predict`, and `seed()` validation in both bootstrap paths. |
| `test_finegray_mi.do` | `mi estimate, cmdok:` compatibility: no unregistered `_fg_*` columns, bit-identical estimates off `mi` data, fail-closed post-estimation, detection in every `mi` style, hand-computed Rubin pooling, the hostile `_mi_*`-lookalike negatives, and the typed-weight pass-through. |
| `test_finegray_tvc.do` | `tvc()`/`tsplit()` piecewise-constant beta(t): external equivalence (`stcrreg, tvc() texp()` and a hand-split episode Cox fit), the tie convention, the coefficient stripe and `e()` contract, every rebuild and prediction path, determinism, cost shape, and every refusal. |
| `test_finegray_bstrata.do` | `bstrata()` baseline stratification: one-level bit-identity, the `stcox, strata()` equivalence oracle, the K x 3 `e(basehaz)` layout, replay, the remaining refusals, the `bstratum()`/`basecshazard` contracts, the event-free-stratum contract, and the stratified-`psi` arms. |
| `test_finegray_tvc_bstrata.do` | The `tvc()` x `bstrata()` composition: the `stcox, strata()` split-episode oracle, the two degenerate identities bit for bit, a hand CIF from the K x 3 `e(basehaz)`, per-stratum prediction and baseline checks, replay, the duplicate-stratum halving, determinism, and bootstrap. |
| `test_finegray_tvc_bstrata_perf.do` | The `tvc()` x `bstrata()` baseline assembly makes exactly one risk-set scan per interval and none per stratum, asserted from the Mata scan counter at two stratum counts, plus the stratum-major layout of `e(basehaz)`. |
| `test_finegray_weights.do` | `[pweight=]`/`[fweight=]`: unit-weight bit-identity, the two in-Stata oracles (expanded fweight fit, clustered pweight fit), the constant-pweight likelihood identity, every baseline/prediction/CIF path, the `e()` weight contract, the data-level refusals, and the post-estimation weight reconciliation (total, digest, compensated change, subject exchange, missing `id()`). |
| `test_finegray_wsig_legacy.do` | Weighted post-estimation on an `e()` that carries no `e(wsig)` digest: a genuinely pre-digest build is installed and its compensated- and permuted-weight changes must produce the audible degradation warning at `rc 0`, while the same changes with the digest present stay `r(459)`. |
| `test_finegray_receipts.do` | The baseline-cache receipts: `e(bh_seq)` is a positive integer that advances by one per fit and survives `estimates store`/`restore`, and `mata clear` resets it to 1 while `e(bh_key)` stays unrepeatable. |
| `test_finegray_fences.do` | The whole option-lattice fence matrix in one place, asserted on (return code, option-name tokens) rather than on message wording, plus the disclosure macros. |
| `test_finegray_mi_lattice.do` | Every v1.2.0/v1.3.0 mechanism under `mi`, in both reachability modes, including hand Rubin pooling, the style x context detection matrix, `mi update`/`datasignature`, replay, and the post-estimation `r(301)` refusals. |
| `test_finegray_adversarial_v130.do` | Cross-feature, hostile, and degenerate probes for the v1.3.0 surface on the axes the per-feature suites do not cover: `mi`, `tvc()`, `bstrata()`, and the scale-independence of the two data-shape rules. |
| `test_finegray_adversarial_v120.do` | Cross-feature and boundary probes for the v1.2.0 surface: a hand-built delayed-entry partial likelihood, zero-length episodes, an empty early risk set, degenerate `truncstrata()`, `nuisance` with two clusters, genuinely failing bootstrap replications, and the scoped missing-`compete()` refusal. |
| `test_finegray_errors.do` | Public error contracts: early option conflicts in the fit, mid-path CIF grid conflicts and the fitted state they must preserve, the PH-diagnostic option contract, and ignored-option probes in prediction. |
| `test_finegray_release120.do` | VCE contradictions, clustered bootstrap identities, raw residuals, and cluster-label invariance. |
| `test_finegray_ties.do` | Censor/event tie conventions and delayed-entry risk-set boundaries. |
| `test_finegray_optimizer.do` | Rank, convergence, tolerance, accepted-likelihood, and Newton-decrement safeguards. |
| `test_finegray_variance.do` | Robust, clustered, model-based, adjusted, and rank-aware variance contracts, including the finite-sample factor as a coefficient-variance convention that leaves every analytic CIF standard error untouched. |
| `test_finegray_bootstrap.do` | Refit fidelity, sample qualifiers, replication floors, seeds, and failed-refit handling. |
| `test_finegray_postest.do` | Factor rebuilds, stale-data checks, CIF limits, baseline grids, post-estimation cleanup, the `mata clear` cache-key regression, and the covariate-free `basecshazard` reads. |
| `test_finegray_nuisance_lt.do` | `nuisance` under delayed entry (ZZF 2011 Appendix B): convergence of the three-term representation to eta+psi, the pooled-weight contract, the stratified-weight and `norobust` fences, replay, determinism, mean-zero influence rows, the `cluster()` composition, and the joint `Variance:` line. |
| `test_finegray_cif_over.do` | `finegray_cif, over()`: bit-identity of every overlaid curve with its standalone call, the stacked `r()` surface, the refusals, the unchanged surface without `over()`, and exact (`%21x`) level transport. |
| `test_finegray_margins.do` | Native `margins` on factor terms and the widened stripe behind it: the posted `e(b)`/`e(V)` and accessors, agreement with `at()` and hand-computed counterfactual means, `dydx()` by hand from `e(V)`, every rebuild and restore path, and the downstream `test`/`contrast`/`lincom` family. |
| `test_finegray_zzf.do` | Delayed-entry Weight 1 parsing, support, diagnostics, limiting cases, warnings, and refit fidelity. |
| `test_finegray_fvgrammar.do` | Base-none factors, interactions, missing values, and unseen-level refusal. |
| `test_finegray_at_profile.do` | `at()` covariate profiles on factor and interaction designs, including raw-variable propagation and direct `_fg_*` overrides. |
| `test_finegray_fg03_diagnostic.do` | `finegray_phtest` matrix/display semantics and no-variation refusal. |
| `test_finegray_fg06_vce.do` | Delayed-entry variance labeling and whole-fit coefficient bootstrap. |
| `test_finegray_fg07_options.do` | Accepted and refused post-estimation option combinations. |
| `test_finegray_nuisance.do` | Estimated-censoring-weight influence terms, score invariants, clustering, and Cox reduction. |
| `test_finegray_determinism.do` | Bit-identical repeat calls and post-estimation call-order independence. |
| `test_finegray_reporting.do` | Factor-aware profiles and complete analytic/bootstrap CIF reporting. |
| `test_finegray_contracts.do` | Weight-stratum mapping, factor scoring, singleton strata, and fail-closed inversion. |
| `test_finegray_nullcase.do` | Fail-open contracts (Critical Rules 14/15): degenerate artifacts that travel the whole command and produce nothing must exit nonzero and leave no half-written variable behind. Each refusal is paired with a positive control. |
| `test_finegray_horizon_precision.do` | Explicit `attime()`/`timepoints()` horizons are used as typed: known answers at, one ulp before, and one ulp after a tied cause event; two horizons that round to the same nine digits stay two rows; duplicates collapse by value; literals beside ranges keep their precision; the default grid and `predict` agree at the event. |
| `test_finegray_cif_overflow.do` | `exp(xb)` overflow at a finite `at()` profile: `finegray_cif` posts CIF 1 with SE 0 and no limits (never a missing CIF with a zero SE), identical to the finite row one step short of the overflow; bootstrap SD finite; `finegray_predict, cif` gives 1 after the first event and 0 before it; the piecewise `tvc()` paths agree; a nonfinite profile is refused. |
| `test_finegray_failclosed.do` | Missing-injection regressions for the QA guard constructs themselves (FG-08A): every guard in `FAILCLOSED_GUARD_MAP.md` gets a fixture where the guarded quantity is missing, the unguarded form passes on it, and the guard exits nonzero. |
| `test_finegray_hostile.do` | No-event and foreign-postestimation refusal with input preservation. |
| `test_finegray_estimates_use.do` | Saved-estimate reloads for datasets saved before and after the fit, empty `e(sample)` diagnosis, `estimates esample:` recovery, and signature enforcement. |
| `test_finegray_sthlp_render.do` | Self-contained SMCL render, literal-markup, rendered-whitespace, `finegray_methods##marker` link-target, and `{synopt}` column-width checks for shipped help, each paired with a fault injection. |
| `test_documentation_examples.do` | Installed-user execution of README/help workflows and advertised baseline options. |

### Validation

| File | Covers |
|---|---|
| `validation_finegray.do` | Hand-computed invariants and native `stcrreg` comparisons. |
| `validation_finegray_recovery.do` | Large-sample recovery of a known Fine-Gray log-SHR. |
| `validation_finegray_recovery_paths.do` | Known-truth recovery across coding, option, estimand, censoring, and multiple-record paths. |
| `validation_cluster_recovery.do` | Zhou et al. (2012) positive-stable shared-frailty DGP with exact marginal truth `beta = alpha*tau`: generator validation, coefficient recovery under two covariate designs, rejection of the conditional frailty coefficient as the estimand, and the study's opposite clustered-SE directions. |
| `validation_finegray_cif_recovery.do` | Analytic CIF recovery at reference and nonzero profiles, including plateau and bounds. |
| `validation_finegray_cif_se.do` | Deterministic analytic CIF inference and delete-one sensitivity envelopes, plus one exactness cell on uncensored data where the analytic and jackknife SEs estimate the same quantity. Section 10 replaces the envelope on the ORDINARY branch with an exact oracle: the fixed-weight CIF influence function is re-derived and recoded from scratch in Mata inside the file (weights, risk-set sums, Breslow baseline, information, score residuals and influence functions all rebuilt from `_t`, `_d`, `status` and `e(b)`; no package Mata routine is called), and compared with the shipped analytic SE on two covariate profiles x three horizons, censored and uncensored. 10a first identifies the shipped weight's G convention by measurement -- the left limit `G(u-)`, Geskus (2011) eq. 11, reproduces `e(basehaz)` while the right-continuous one does not -- which is what licenses the comparison; 10c injects a fault (drop the coefficient-uncertainty term) and requires it to be detected. Measured 2026-09-04: 1.1e-15 to 2.3e-15 relative. |
| `validation_finegray_lt_cluster_cif_se.do` | Calibrated Monte Carlo for the CLUSTERED, DELAYED-ENTRY analytic CIF SE (FG-09 remainder). No source supplies a CIF influence function under clustering -- Zhou et al. (2012) p.377 says the variance "is rather complicated, with bootstrapping providing practicable inferences", and Fine & Gray (1999) sec. 5 obtains CIF bands by simulation -- so this file calibrates rather than asserting an identity, with the pass rule pre-registered in its header before the run. Zhou et al. (2012) sec. 4.1's positive-stable shared frailty gives an exact closed-form marginal CIF (verified against the generator in G0); 1200 replications at three horizons. Measured 2026-09-04: analytic clustered SE / Monte Carlo SD = 1.032/1.005/1.002 with coverage 0.951/0.956/0.954, against 0.801/0.772/0.776 and ~0.83 coverage for the unclustered SE on the same fits; a matched independent-subject arm returns both to 0.99-1.00 and nominal coverage. Runtime 5m29s. |
| `validation_finegray_lt_se.do` | Delayed-entry score identities and coefficient/CIF delete-one sensitivity envelopes, plus the clustered delayed-entry variance rebuilt outside the package from the score residuals. |
| `validation_pweight_recovery.do` | Known-truth recovery under informative sampling (Wogu et al. 2021 sec. 5): the unweighted fit must be biased and the `[pweight=]` fit unbiased, with sandwich coverage and mean-SE bands, and the same replications gate the weighted analytic CIF interval. Header carries the pre-registered attrition rule; the former fixed coverage `[0.90, 0.99]` and SE/SD `[0.85, 1.15]` bands are replaced by MCSE-derived ones computed from each arm's realized replication count, and NREP is raised 100 → 400 because at 100 the fixed bands were tighter than the Monte Carlo noise justified. |
| `validation_tvc_recovery.do` | Known-truth recovery under `tvc()` from data generated by inverting the piecewise model's own cumulative incidence: Monte Carlo bias, 95% coverage, and mean standard error against the Monte Carlo standard deviation. **Two structural DGPs** (FG-15 remainder, 2026-09-04): arm A exponential censoring (constant hazard) with N(0,1) and Bernoulli(0.4) covariates, arm B uniform administrative censoring (rising hazard) with a standardised chi2(4) covariate and Bernoulli(0.7). The header carries the **pre-registered failure rule** — what attrition rate voids the study and why 98% is the bound, and that the repair for a failure is never a wider band — and every threshold is derived from the arm's own realized replication count (coverage 0.95 ± 3·sqrt(0.95·0.05/m), SE/SD 1 ± 3/sqrt(2m), bias 4 MCSE), with the nominal-95% band printed beside it. 27/27 on 500 + 300 replications, no attrition, 4m25s. |
| `validation_bstrata_recovery.do` | Known-truth recovery of the shared coefficient vector and of each stratum's closed-form cumulative baseline under `bstrata()`, with the pooled fit's single baseline as the discriminating contrast. Arm E's calibration bands were already MCSE-derived; its header now carries the pre-registered attrition rule, including what a 90% survival rate means for the coverage numbers it reports. |
| `validation_finegray_zzf_recovery.do` | Multi-hour known-truth recovery gate for delayed-entry Weight 1. |
| `validation_finegray_zzf_coverage.do` | Multi-hour delayed-entry confidence-interval coverage gate. |
| `validation_finegray_zzf_factorization.do` | Multi-hour mechanism-conditioning, factorization, support, and positivity gate. |

### Cross-validation

| File | Oracle |
|---|---|
| `crossval_predict_stcrreg.do` | Row-level parity with Stata's native `stcrreg` prediction paths. |
| `crossval_finegray.do` | Coefficient, SE, likelihood, CIF, strata, and benchmark parity with `stcrreg` and `cmprsk::crr`, with the stratified numbers cross-validated against `crr(cengroup=)`. |
| `crossval_finegray_dta.do` | Seeded tied-event double-precision `.dta` exchange; coefficient and full nuisance-sandwich SE parity with `cmprsk::crr`. |
| `crossval_cif.do` | CIF parity with `riskRegression` and bootstrap uncertainty checks. |
| `crossval_predict_phtest.do` | Prediction and diagnostic parity with an R implementation. |
| `crossval_finegray_zzf.do` | Dataset-level parity with a regenerated direct-equation ZZF Weight 1 oracle: coefficients on every dataset, the maximised log-likelihood on the pooled-weight arms, and the whole baseline curve on six datasets. |
| `crossval_nuisance.do` | Whole variance/covariance matrix parity for the nuisance-adjusted sandwich. |
| `crossval_pweight.do` | `[pweight=]` parity with `survival::finegray(weights=)` + weighted `coxph` on two fixtures: coefficients, robust and cluster-robust SEs, the weighted log pseudo-likelihood, and the reference CIF from `survfit`. |
| `crossval_tvc.do` | `tvc()`/`tsplit()` coefficient parity with `stcrreg, tvc() texp()` and with `cmprsk::crr` `cov2`/`tf` interval indicators across three fixtures, a `tvc()` x `strata()` arm against `crr`'s `cengroup`, and full-covariance SE comparisons on the `nuisance noadjust` arms. |
| `crossval_tvc_bstrata.do` | Composed-VCE oracle for `tvc()` x `bstrata()` and `tvc()` x `bstrata()` x `cluster()` against `survival::finegray()` + `coxph(weights=fgwt, strata(bs), cluster=, robust=TRUE, ties="breslow")` on the expanded risk set, `survSplit` at the `tsplit()` cuts. That is the cell `finegray_methods.sthlp` had called "validated by simulation rather than against an external implementation": `crr` cannot stratify a baseline at all, and `crrSC::crrs`'s two `ctype`s are the within-stratum-G and the highly-stratified derivations, neither of which is a stratified baseline over a pooled G. Compared on coefficients, name-addressed SEs, and the FULL covariance (`mreldif`), for the `noadjust` fit whose `fixed_weight` meat is the same estimator `coxph`'s robust sandwich computes. Measured 2026-09-04: coefficients 1.6e-10 to 4.2e-10, SEs 6.8e-12 to 1.1e-11, `mreldif(e(V))` 7.5e-13 to 1.9e-12; gated at 1e-7/1e-9, three orders below the smallest wrong-answer separation (dropping `noadjust` 2.0e-06, dropping `bstrata()` 8.8e-05, subject instead of cluster sandwich 2.5e-04), each of which the file also asserts is at least 1000x the agreement. |
| `crossval_bstrata.do` | `bstrata()` coefficient parity with `crrSC::crrs` at both `ctype` mappings across two, three and four strata and with heavy ties, plus the regime-1 `eta + psi` variance as a genuine second implementation. |
| `crossval_public_studies.do` | Regenerates the public `crrSC::bce` and `crrSC::center` examples and gates every coefficient, both comparable full covariance matrices, event counts, sample omissions, and stratum/cluster counts against the paper authors' own fits. |

### Oracle caching

The R cross-validation oracles are computed once per machine and never again
until an input changes. Each `crossval_*_r.R` caches its output under
`<cache>/<name>/<key>/`, where `<cache>` is `$FG_ORACLE_CACHE_DIR` if set and
otherwise R's per-user cache directory for the suite, `tools::R_user_dir("finegray_qa", "cache")`
(`~/.cache/R/finegray_qa` on Linux); `_fg_oracle_cache.R` holds the shared
mechanism. The cache is deliberately outside the package tree: the devkit runs
every lane from a throwaway scratch copy, and a cache kept at `qa/.oracle_cache/`
was discarded with each copy and never filled. Each suite log carries an
`ORACLE CACHE HIT [...] restored from <dir>` or `MISS [...] computing` line per
oracle, so a lane's log shows which oracles were recomputed and why. The key covers the calling script and anything it
sources, the content (never the path) of every input data file, named scalar
parameters, and the R version, platform and version of every package used. Any
key mismatch, missing blob or md5 disagreement runs the real computation. R
still runs on every lane -- the cache lives inside the R scripts, not around
`Rscript` -- so no `.do` file can skip it.

Force a recompute for one run:

```
FG_ORACLE_NOCACHE=1 stata-mp -b do run_all.do full
```

Deleting the cache directory has the same effect for the next run. The cache is
never inside the repository, so nothing about it is tracked. Why it is keyed this way, and the measurement that motivated it,
are in [AUDIT_NOTES.md](AUDIT_NOTES.md#oracle-caching-rationale).

### Support and out-of-band runners

| File or path | Covers |
|---|---|
| `run_all.do` | Curated `quick`, `core`, `python`, `full`, and `gates` lane membership and sentinel enforcement. |
| `_finegray_qa_common.do` | Sandboxed install bootstrap and shared seeded fixture builders. |
| `gates_transfer_proof.do` | Reproducible delayed-entry output comparison between the pinned and current trees. |
| `benchmark_finegray_zzf.do` | Standalone scan scaling measurement; not a correctness gate and run on demand. |
| `benchmark_finegray_crossval.do` | Wall-clock finegray-vs-`stcrreg` timings at N = 500 to 50,000, plus the one coefficient comparison that came with them; not a correctness gate and not a lane member. |
| `_benchmark_finegray_zzf_cell.do` | Fresh-process worker for one benchmark cell. |
| `run_all.sh`, `test_run_all_wrapper.sh`, `test_finegray_fg02_failclosed.sh` | Reliable shell status, provenance/receipts, wrapper regression, and stale-oracle fail-closed checks. |
| `crossval_tvc_r.R`, `crossval_pweight_r.R`, `crossval_bstrata_r.R`, `crossval_public_studies_r.R`, `crossval_cif_r.R`, `crossval_finegray_r.R`, `crossval_finegray_zzf_beta_r.R`, `crossval_finegray_zzf_r.R`, `crossval_nuisance_r.R`, `crossval_predict_phtest_r.R` | Independent R generators and comparison implementations used by `crossval_*`. |
| `validation_finegray_zzf_prereg_r.R` | Independent preregistration of the recovery gate's signed controls; deliberately not a lane member, and re-derived with `cd finegray/qa && Rscript validation_finegray_zzf_prereg_r.R`. |
| `gates_transfer_pin.txt`, `run_all_status.txt`, `run_status_full.txt`, `run_status_gates.txt` | Transfer baseline and machine-readable lane receipts. |
| `_r_environment.txt`, `.gitignore` | Recorded external environment and generated-artifact policy. |

## Coverage map

| Command | Functional | Validation | Cross-val | Also exercised in |
|---|---|---|---|---|
| `finegray` | `test_finegray*`, optimizer/variance/bootstrap/ZZF/nuisance/`bstrata`/`tvc`/`tvc_bstrata`/`weights` suites, the fence matrix, the `mi` lattice probe, and the two `test_finegray_adversarial_*` suites | Recovery, clustered recovery, CIF, LT-SE, `bstrata` recovery, `tvc` recovery, `pweight` recovery, and ZZF gate suites | `crossval_finegray*`, `crossval_bstrata`, `crossval_public_studies`, `crossval_tvc`, `crossval_pweight`, `crossval_nuisance`, `crossval_predict_stcrreg` | Documentation examples, contracts, determinism, ties; adversarial cross-feature (`mi` x `bstrata()`, `mi` x `tvc()`, `tvc()` x `strata()` x `cluster()`), namespace, hostile-label, and degenerate-mass probes |
| `finegray_predict` | Postestimation, factor-grammar, options, reporting, v1.1/v1.2.1 regressions | CIF recovery and CIF/LT-SE suites | `crossval_predict_phtest`, `crossval_predict_stcrreg`, `crossval_cif` | Determinism, saved-estimate reloads; adversarial `mi` fail-closed and long-name `tvc()` prediction |
| `finegray_cif` | CIF/bootstrap/reporting/options, `at()` profiles on factor and interaction designs, `over()` overlay identity, and v1.1/v1.2.1 regressions | CIF recovery and CIF/LT-SE suites | `crossval_cif`, `crossval_finegray`, `crossval_predict_stcrreg` | Documentation examples, determinism, saved-estimate reloads; adversarial `bstratum()` addressing, empty-risk-set CIF, and bootstrap-failure honesty |
| `finegray_phtest` | Diagnostic, factor, postestimation, and determinism suites | Core invariant suite | `crossval_predict_phtest` | Documentation examples, saved-estimate reloads |

## Lane membership

`quick` is contained in `core`, and `core` plus the adversarial and external-oracle suites forms `full`; `full` is the default release gate. The explicit per-suite lists in `run_all.do` are authoritative.

| Lane | Suites |
|---|---|
| `quick` | All curated functional/regression `test_*` suites except the two adversarial suites; includes the fence matrix, the `mi` lattice probe, the `tvc()` x `bstrata()` suite and the design-weight suite. |
| `core` | `quick`, deterministic/known-truth validation, and native `stcrreg` prediction parity. |
| `python` | The R-backed `crossval_*` suites; the name is retained for compatibility. |
| `full` | `core`, the two `test_finegray_adversarial_*` suites, plus `python`; default `run_all.do` and release-wrapper gate. |
| `gates` | The three multi-hour ZZF recovery, coverage, and factorization validations, run on demand. |
| Shell gates | Wrapper regression on non-`gates` lanes; stale-oracle gate on `python`/`full`; delayed-entry transfer proof on `full`/`gates`. |
| Benchmark | `benchmark_finegray_zzf.do` and `benchmark_finegray_crossval.do`, run manually and never interpreted as a correctness verdict. |

## Known gaps

- **`stata-mp -b do run_all.do <lane>` does not run the three shell gates.** The fail-closed (`fg02`), wrapper-regression and delayed-entry transfer gates live in `run_all.sh`, not in `run_all.do`, so the Stata runner — and the devkit CLI, which drives `run_all.do` — reports a green lane with none of them executed. The same is true of `run_all_status.txt` when it was last written by a `run_all.sh` run: the Stata runner does not write it. **A release receipt must come from `./run_all.sh full --source-repo <git checkout>`**, run from a scratch copy, and nothing else counts as one. Without `--source-repo` the transfer gate has no git tree to extract the pinned commit from; that reports `NOT-RUN` and fails the `full` and `gates` lanes rather than passing them silently.
- `full` intentionally excludes the multi-hour `gates` lane and scaling benchmark; their separate receipts and transfer pin do not substitute for rerunning them after an estimator-core change.
