# msm QA fixtures manifest

Provenance, licence, checksum, transformations, and regeneration for every
dataset the QA lane reads. Governed per audit Q07. Checksums are MD5 of the
committed file; regenerate them with `md5sum` after any deliberate change.

## Tracked fixtures

| File | Rows / shape | Source & licence | MD5 | Transformations | Regeneration |
|---|---|---|---|---|---|
| `../msm_example.dta` | 4,586 person-periods, 500 ids, periods 0–9 | Synthetic, generated for this package (no external data; no licence constraint) | `25115665578f38c4e242b17bd7c75623` | None — used as-shipped by the pipeline examples and 17 QA suites | Deterministic generator not committed; the file is the canonical artifact (also shipped to users via `net get msm`) |
| `data/nhefs.dta` | 1,629 subjects, one row per subject | NHEFS — the National Health and Nutrition Examination Survey Epidemiologic Follow-up Study teaching extract distributed with Hernán & Robins, *Causal Inference: What If* (public, non-commercial teaching use; https://www.hsph.harvard.edu/miguel-hernan/causal-inference-book/) | `3dfc7dc06acb9d2b565bebb63874174a` | Used as-downloaded in `validation_msm.do` §V2/Ch.17; the person-period MSM panel is rebuilt **on the fly** into a tempfile, not stored | Re-download from the Causal Inference book site |
| `data/haartdat.dta` | HAART-initiation survival data, person-period | Public teaching dataset for IPW/MSM survival examples in the Hernán/Cole IPW literature (Cole & Hernán 2008 style HAART/zidovudine survival MSM; non-commercial teaching use) | `8d4cac22e5a79be70d6cc70afbf2f19c` | Restructured inside `validation_msm.do` §V2 for the R IPW cross-validation (weight mean ~1.04, protective HAART coefficient) | Re-download from the source distribution |

## Generated fixtures (not committed)

| Path | Contents | Regeneration |
|---|---|---|
| `crossval_data/dgp{1,2,3}_*.dta` and `.csv` | DGP panels for the cross-language parity suites (`crossval_msm`, `crossval_external_models`) | Regenerated by `_crossval_dgp_generate.do`; the directory is git-ignored and rebuilt by the crossval lane |

## Notes

- `nhefs_personperiod.dta` (an 8.3 MB pre-expanded person-period copy of NHEFS)
  was an **untracked orphan**: `validation_msm.do` rebuilds the person-period
  panel from `nhefs.dta` into a `tempfile`, so the disk copy was never loaded.
  Removed 2026-07-18 (audit Q07).
- Only `msm_example.dta` is shipped to users (via `net get msm`); the `data/`
  fixtures are QA-only and are not in `msm.pkg`.
