# setools QA fixture manifest

| Fixture | Purpose and schema | Source/license | Generator/refresh | SHA-256 |
| --- | --- | --- | --- | --- |
| `data/cci_authoritative_prefixes.csv` | 1,925 cases with `case_id`, ICD era/year, input code, case kind, expected total score, and 18 expected public components. Covers every prefix parsed from the pinned implementation, separator/case variants, malignancy range holes, and boundaries. | Björn Roelstraete/Jonas Söderling Stata implementation for Ludvigsson et al.; pinned Git commit `0f2b6058fa551deb6d4b68116a21551f13ef99bc`, upstream SHA-256 `ed865ef10f2d0f29235091ec6ec1e921b2db39b17cf9e37624f2b75f84868acd`. Used for verification with upstream attribution. | Download the pinned `Charlson_STATA` source, verify its hash, then run `python3 tools/build_cci_authoritative_fixture.py --source /path/Charlson_STATA --output data/cci_authoritative_prefixes.csv`. Review any changed vector before updating this manifest. | `0db432499f2fdc70efbba4ef979ff7e321c41314d0620390b99c3ed73e2d5e27` |
| `data/edss_long.dta` | Synthetic repeated-visit EDSS data: 6,003 rows, 1,000 IDs, unique `id edss_dt`, `%td` assessment dates, and EDSS values within 0–10. Consumed by `test_edss_fixture.do`. | Generated locally; MIT license with the setools package; no patient or external data. | From `qa/`, run `stata-mp -b do tools/build_edss_fixture.do data/edss_long.dta`, rerun `test_edss_fixture.do`, then record the new hash if Stata’s binary serialization changes. | `a3b1cabbffa975f8c675feb70c469cd0ddd0657e699d8296ce5ae01630841e16` |

Do not add a fixture without a schema, provenance/license statement, deterministic refresh command, consuming suite, and recorded hash. Live URLs belong only in `test_network_smoke.do` and must be pinned and checksum-verified.
